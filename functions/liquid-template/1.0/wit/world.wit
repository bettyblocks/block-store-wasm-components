package betty-blocks:liquid-template@1.0.0;

interface liquid-template {
    record variable {
        %key: string,
        %value: string
    }

    liquid-template: func(template: option<string>, template-variable: option<string>, variables: string) -> result<string, string>;
}

world main {
    export liquid-template;
}

