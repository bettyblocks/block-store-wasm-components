package betty-blocks:concat-text@1.0.0;

interface concat-text {
    concat-strings: func(strings: list<string>) -> string;
    concat-strings-with-separator: func(strings: list<string>, separator: string) -> string;
}

world main {
    export concat-text;
}
    
